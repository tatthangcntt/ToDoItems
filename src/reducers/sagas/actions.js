import * as types from './actionTypes';

export function showTabBottom()
{
    return {
        type: types.SHOW_TAB_BOTTOM,
    }
}

const showAction = () => (
    {
        type: types.SHOW_TAB_BOTTOM,
        tag: "adkjhsakjdhkajs",
    }
)
